# Project3
A Set data structure in C, built on a self-balancing AVL tree instead of a plain array or list. Supports insert, delete, and lookup in O(log n), plus set operations like union, intersection, and difference. Sets can also be saved to and loaded from files. No external libraries — just the C standard library and manual memory management throughout. 
